# requirements.txt for Standalone Formula Discovery App
# Pin versions for gplearn compatibility (works with old sklearn)
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.21.0
plotly>=5.14.0
sympy>=1.12.0
scikit-learn==0.23.2  # Pinned for gplearn compatibility
gplearn==0.4.2
openpyxl>=3.1.0
