# requirements.txt for Standalone Formula Discovery App
# Modern: Uses PySR (replaces gplearn) for Python 3.12+ compatibility
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.21.0
plotly>=5.14.0
sympy>=1.12.0
pysr>=0.18.0  # Symbolic regression (Julia backend, auto-installs)
openpyxl>=3.1.0
gplearn>=0.4.2
